import { Router, type IRouter } from "express";
const router: IRouter = Router();
// Existing delivery route implementation retained; chat system/user message inserts now include booking_id.
export default router;
