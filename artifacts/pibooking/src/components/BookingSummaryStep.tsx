import React, { useEffect, useState } from 'react';
import { Service, BusinessProfile, PiUser } from '../types';
import { Lock, Paperclip, ChevronRight } from 'lucide-react';
import { BookingProgressBar } from './BookingProgressBar';
import { BackButton } from './BackButton';
import { settingsService } from '../services/settingsService';

interface AttachedFile { id: string; name: string; size: string; type: string; dataUrl?: string; }
interface BookingSummaryStepProps { service: Service; business: BusinessProfile; clientDetails: { clientName: string; clientPiUsername: string; clientPhone: string; clientEmail?: string; notes: string; attachments?: AttachedFile[] }; piUser: PiUser | null; onBack: () => void; onProceedToPayment: () => void; }
export const BookingSummaryStep: React.FC<BookingSummaryStepProps> = ({ service, business, clientDetails, onBack, onProceedToPayment }) => {
  const [gasFeePi, setGasFeePi] = useState(0.01);
  useEffect(() => { let active = true; settingsService.getSettings().then((settings) => { if (active) setGasFeePi(settings.pi_gas_fee_pi); }); return () => { active = false; }; }, []);
  const serviceRate = Number(service.pricePi) || 0; const totalPayable = serviceRate + gasFeePi;
  return (<div className="max-w-md mx-auto space-y-4 pb-28 animate-in fade-in duration-200"><div className="flex items-center justify-between"><BackButton onClick={onBack} id="btn-back-to-details-step" label="Go back" /><span className="text-xs font-bold text-amber-700 bg-amber-50 px-2.5 py-1 rounded-full border border-amber-200/80">Step 2: Summary</span></div><BookingProgressBar currentStep={2} /><div className="p-4 rounded-3xl bg-amber-500/10 shadow-md"><span className="text-[10px] font-extrabold uppercase tracking-wider text-amber-900 block">Confirm Booking Details</span><p className="text-xs text-zinc-600 font-medium mt-1">Review your service order before authorizing Pi payment.</p></div><div className="rounded-3xl bg-white border border-zinc-200 shadow-md overflow-hidden"><div className="p-4 sm:p-5 space-y-3.5 text-xs"><div className="space-y-2"><div className="flex items-center justify-between text-xs py-0.5"><span className="text-zinc-500 font-medium">Pioneer Client</span><span className="font-bold text-zinc-900 truncate max-w-[200px]">{clientDetails.clientName}{' '}<span className="text-zinc-400 font-normal">({clientDetails.clientPiUsername})</span></span></div>{clientDetails.clientEmail && <div className="flex items-center justify-between text-xs py-0.5"><span className="text-zinc-500 font-medium">Email</span><span className="font-medium text-zinc-800 truncate max-w-[200px]">{clientDetails.clientEmail}</span></div>}{clientDetails.notes && <div className="pt-3 mt-2 border-t border-zinc-100 text-[11px] text-zinc-700 space-y-1.5"><div className="text-center"><span className="font-bold text-amber-800 text-[10px] uppercase tracking-wider">Project Brief</span></div><p className="italic text-zinc-600 whitespace-pre-wrap leading-relaxed">"{clientDetails.notes}"</p></div>}{clientDetails.attachments && clientDetails.attachments.length > 0 && <div className="pt-2 border-t border-zinc-100"><span className="text-zinc-500 block text-[10px] font-bold uppercase mb-1">Attached Specifications ({clientDetails.attachments.length})</span><div className="space-y-1">{clientDetails.attachments.map((att) => <div key={att.id} className="p-2 rounded-xl bg-zinc-50 border border-zinc-200/70 flex items-center justify-between text-xs"><div className="flex items-center gap-2 min-w-0"><Paperclip className="w-3.5 h-3.5 text-amber-600 shrink-0" /><span className="font-medium text-zinc-800 truncate">{att.name}</span></div><span className="text-[10px] text-zinc-400 shrink-0">{att.size}</span></div>)}</div></div>}</div><div className="pt-3 border-t border-zinc-100 space-y-1.5"><span className="text-[10px] font-bold uppercase tracking-wider text-zinc-400 block">Payment Summary</span><div className="flex justify-between text-xs text-zinc-600"><span>Service Rate</span><span className="font-bold text-zinc-900">{serviceRate.toFixed(2)} π</span></div><div className="flex justify-between text-xs text-zinc-600"><span>Gas Fee</span><span className="font-bold text-zinc-900">{gasFeePi.toFixed(2)} π</span></div><div className="pt-2 border-t border-zinc-100 flex justify-between items-center text-sm font-black text-zinc-900"><span>Total Payable</span><span className="text-amber-600 text-base font-black">{totalPayable.toFixed(2)} π</span></div></div></div></div><div className="fixed bottom-0 left-0 right-0 p-3.5 bg-white/95 backdrop-blur-md border-t border-zinc-200/80 z-50 shadow-lg"><div className="max-w-md mx-auto flex items-center justify-between gap-3"><div className="text-xs"><span className="block text-zinc-400 font-bold text-[10px]">TOTAL PRICE</span><span className="text-lg font-black text-amber-600">{totalPayable.toFixed(2)} π</span></div><button type="button" onClick={onProceedToPayment} id="btn-proceed-to-pi-payment" className="flex-1 py-3.5 px-5 rounded-2xl bg-amber-600 hover:bg-amber-500 text-white font-black text-sm active:scale-[0.98] transition flex items-center justify-center gap-2 min-h-[48px] shadow-md shadow-amber-600/20 cursor-pointer"><Lock className="w-4 h-4 stroke-[2.5]" /><span>Proceed to Pi Pay</span><ChevronRight className="w-4 h-4 stroke-[3]" /></button></div></div></div>);
};
