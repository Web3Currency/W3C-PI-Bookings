import React, { useEffect, useState } from 'react';
import { Booking } from '../types';
import { CalendarCheck, ChevronRight, Clock, Search, ArrowUpDown, AlertCircle, Star } from 'lucide-react';
import { ClientAcceptanceBanner, clientStatusBadge } from './ClientAcceptanceBanner';
import { piAuthService } from '../services/piAuthService';
import { AuthRequiredView } from './AuthRequiredView';
import { formatBookingCreated } from '../lib/utils';
import { ClientBookingReceipt } from './ClientBookingReceipt';
interface BookingStatusViewProps { bookings: Booking[]; onBrowseServices: () => void; onCancelBooking: (bookingId: string) => void; onAddReview?: (bookingId: string, rating: number, comment: string) => void; onConfirmCompletion?: (bookingId: string) => void; onOpenChat: (bookingId: string) => void; }
export const BookingStatusView: React.FC<BookingStatusViewProps> = ({ bookings, onBrowseServices, onCancelBooking, onAddReview, onConfirmCompletion, onOpenChat }) => {
  const [selectedBookingId, setSelectedBookingId] = useState<string | null>(null); const [filter, setFilter] = useState<'all' | 'active' | 'completed'>('all'); const [searchQuery, setSearchQuery] = useState(''); const [sortOrder, setSortOrder] = useState<'newest' | 'oldest' | 'price_high'>('newest'); const [reviewRating, setReviewRating] = useState(5); const [reviewComment, setReviewComment] = useState(''); const [reviewSubmitted, setReviewSubmitted] = useState(false);
  useEffect(()=>{const id=sessionStorage.getItem('w3c_open_booking_details');if(id&&bookings.some(b=>b.id===id)){setSelectedBookingId(id);sessionStorage.removeItem('w3c_open_booking_details');}},[bookings]);
  const isSignedIn = Boolean(piAuthService.getStoredUser());
  const filteredBookings = bookings.filter((b) => { if (filter === 'active' && !['Confirmed', 'In Progress', 'Pending', 'Delivered'].includes(b.status)) return false; if (filter === 'completed' && !['Completed', 'Cancelled'].includes(b.status)) return false; const q = searchQuery.trim().toLowerCase(); if (!q) return true; return [b.id, b.serviceName, b.status, b.providerName, b.clientName, b.piPaymentId].some((value) => String(value || '').toLowerCase().includes(q)); });
  const sortedBookings = [...filteredBookings].sort((a, b) => sortOrder === 'oldest' ? new Date(a.createdAt || 0).getTime() - new Date(b.createdAt || 0).getTime() : sortOrder === 'price_high' ? Number(b.pricePi || 0) - Number(a.pricePi || 0) : new Date(b.createdAt || 0).getTime() - new Date(a.createdAt || 0).getTime());
  const activeBooking = bookings.find((b) => b.id === selectedBookingId) || null;
  if (!isSignedIn) return <AuthRequiredView title="Sign in to view your bookings" description="Your bookings and order details are private to your Pi Network account. Sign in to view and manage your bookings." onSignIn={() => window.dispatchEvent(new CustomEvent('w3c:sign-in-request'))} />;
  if (activeBooking) { const handleReviewSubmit = (e: React.FormEvent) => { e.preventDefault(); if (onAddReview) onAddReview(activeBooking.id, reviewRating, reviewComment); setReviewSubmitted(true); setTimeout(() => setReviewSubmitted(false), 3000); }; return <div className="space-y-5"><ClientBookingReceipt booking={activeBooking} onBack={() => setSelectedBookingId(null)} onOpenChat={onOpenChat} onCancelBooking={onCancelBooking} onConfirmCompletion={onConfirmCompletion} />{activeBooking.status === 'Completed' && <section className="max-w-3xl mx-auto p-5 rounded-sm bg-white border border-zinc-200 shadow-sm"><div className="flex items-center justify-between mb-4"><h2 className="text-[10px] font-black uppercase tracking-[0.16em] text-zinc-500 flex items-center gap-1.5"><Star className="w-4 h-4 text-amber-500 fill-amber-500" /> Service Review</h2><span className="text-[9px] text-zinc-400 font-bold uppercase tracking-wider">Verified Client Review</span></div>{activeBooking.rating || reviewSubmitted ? <div className="text-center py-3"><div className="flex justify-center gap-1">{[1,2,3,4,5].map((star) => <Star key={star} className={`w-5 h-5 ${star <= (activeBooking.rating || reviewRating) ? 'text-amber-500 fill-amber-500' : 'text-zinc-300'}`} />)}</div><p className="text-xs font-black text-emerald-700 mt-2">Thank you. Your review has been recorded.</p>{(activeBooking.reviewComment || reviewComment) && <p className="text-xs text-zinc-600 italic mt-1">"{activeBooking.reviewComment || reviewComment}"</p>}</div> : <form onSubmit={handleReviewSubmit} className="space-y-3"><p className="text-xs text-zinc-600">Rate your experience with <strong>{activeBooking.serviceName}</strong>.</p><div className="flex items-center gap-1">{[1,2,3,4,5].map((star) => <button key={star} type="button" onClick={() => setReviewRating(star)} className={`p-1 rounded-lg ${star <= reviewRating ? 'text-amber-500 bg-amber-50' : 'text-zinc-300'}`}><Star className={`w-6 h-6 ${star <= reviewRating ? 'fill-amber-500' : ''}`} /></button>)}<span className="text-xs font-black text-amber-600 ml-2">{reviewRating} / 5</span></div><textarea rows={3} value={reviewComment} onChange={(e) => setReviewComment(e.target.value)} placeholder="Share your experience..." className="w-full p-3 rounded-xl bg-zinc-50 border border-zinc-200 text-xs outline-none focus:border-amber-500" /><button type="submit" className="w-full py-3 rounded-xl bg-amber-600 hover:bg-amber-500 text-white text-xs font-black">Submit Review</button></form>}</section>}</div>; }
  if (bookings.length === 0) return <div className="max-w-md mx-auto text-center py-16 px-4 space-y-4"><div className="w-16 h-16 mx-auto rounded-full bg-amber-50 text-amber-600 border border-amber-200 flex items-center justify-center"><CalendarCheck className="w-8 h-8" /></div><div><h2 className="text-xl font-black text-zinc-900">No Bookings Yet</h2><p className="text-xs text-zinc-500 max-w-xs mx-auto mt-1">You haven't booked any Web3 services yet. Connect with verified service providers and book securely using Pi Escrow.</p></div><button type="button" onClick={onBrowseServices} className="py-3 px-6 rounded-2xl bg-amber-600 hover:bg-amber-500 text-white font-black text-xs">Browse Marketplace</button></div>;
  return <div className="max-w-2xl mx-auto space-y-5 pb-24 animate-in fade-in duration-200"><div className="space-y-1"><div className="flex items-center justify-between"><h1 className="text-xl font-black text-zinc-900">My Bookings</h1><button type="button" onClick={onBrowseServices} className="text-xs font-bold text-amber-700">+ Book New Service</button></div><p className="text-xs text-zinc-500 font-medium">{bookings.length} service order{bookings.length > 1 ? 's' : ''} stored on Pi Network</p></div><div className="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3"><div className="flex items-center gap-1.5 overflow-x-auto no-scrollbar">{([{ id: 'all', label: 'All', count: bookings.length }, { id: 'active', label: 'Active', count: bookings.filter((b) => ['Confirmed','In Progress','Pending','Delivered'].includes(b.status)).length }, { id: 'completed', label: 'Completed', count: bookings.filter((b) => ['Completed','Cancelled'].includes(b.status)).length }] as const).map((tab) => <button key={tab.id} type="button" onClick={() => setFilter(tab.id)} className={`px-3 py-1.5 rounded-full text-xs font-extrabold transition shrink-0 ${filter === tab.id ? 'bg-amber-600 text-white' : 'bg-zinc-100 text-zinc-600 hover:bg-zinc-200'}`}>{tab.label} <span className="ml-1">{tab.count}</span></button>)}</div><div className="flex items-center gap-2 w-full sm:w-auto"><div className="relative flex-1 sm:w-44"><Search className="absolute left-2.5 top-1/2 -translate-y-1/2 w-3.5 h-3.5 text-zinc-400" /><input type="search" value={searchQuery} onChange={(e) => setSearchQuery(e.target.value)} placeholder="Search bookings" className="w-full bg-zinc-100 border border-zinc-200 rounded-xl pl-8 pr-2.5 py-1.5 text-xs outline-none focus:border-amber-500" /></div><div className="flex items-center gap-1 text-xs text-zinc-500"><ArrowUpDown className="w-3.5 h-3.5" /><select value={sortOrder} onChange={(e) => setSortOrder(e.target.value as any)} className="bg-zinc-100 border border-zinc-200 rounded-xl px-2.5 py-1.5 text-xs font-bold outline-none"><option value="newest">Newest First</option><option value="oldest">Oldest First</option><option value="price_high">Highest Price</option></select></div></div></div>{sortedBookings.length === 0 ? <div className="p-8 rounded-3xl bg-zinc-50 border border-zinc-200 text-center"><AlertCircle className="w-8 h-8 text-zinc-400 mx-auto" /><h3 className="text-sm font-bold mt-2">No Bookings Found</h3><p className="text-xs text-zinc-500 mt-1">No booking records match the current filters.</p><button type="button" onClick={() => { setFilter('all'); setSearchQuery(''); }} className="mt-3 px-4 py-2 rounded-xl bg-zinc-200 text-xs font-bold">Clear Filters</button></div> : <div className="divide-y divide-zinc-200 border-y border-zinc-200">{sortedBookings.map((b) => { const badge = clientStatusBadge(b); return <button type="button" key={b.id} onClick={() => setSelectedBookingId(b.id)} className="group w-full text-left py-4 px-1 hover:bg-zinc-50/70 transition"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-2 min-w-0"><span className={`inline-flex items-center gap-1.5 px-2.5 py-0.5 rounded-full text-[10px] font-extrabold border ${badge.classes}`}><span className={`w-1.5 h-1.5 rounded-full ${badge.dot}`} />{badge.label}</span></div><span className="text-base font-black text-amber-600 shrink-0">{Number(b.pricePi || 0).toFixed(2)} π</span></div><div className="flex items-center justify-between gap-3 mt-2.5"><h3 className="text-sm font-black text-zinc-900 group-hover:text-amber-700 truncate">{b.serviceName}</h3><span className="flex items-center gap-1 text-[11px] font-bold text-amber-700 shrink-0">View Details<ChevronRight className="w-4 h-4" /></span></div><div className="flex items-center gap-1.5 text-zinc-600 text-xs mt-2"><Clock className="w-3.5 h-3.5 text-amber-600" />Created: {formatBookingCreated(b.createdAt)}</div><ClientAcceptanceBanner booking={b} compact /></button>; })}</div>}</div>;
};