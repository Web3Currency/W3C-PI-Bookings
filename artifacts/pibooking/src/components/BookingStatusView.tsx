import React, { useState } from 'react';
import { Search, ArrowUpDown, ChevronRight, Clock, AlertCircle } from 'lucide-react';
import { Booking } from '../types';
import { formatBookingCreated } from '../lib/utils';
import { ClientAcceptanceBanner } from './ClientAcceptanceBanner';
import { BookingDetailsView } from './BookingDetailsView';
import { bookingService } from '../services/bookingService';
interface BookingStatusViewProps { bookings: Booking[]; onBrowseServices: () => void; onCancelBooking: (id: string) => Promise<void> | void; onAddReview: (id: string, rating: number, comment: string) => Promise<void> | void; onConfirmCompletion?: (id: string) => Promise<void> | void; onOpenChat?: (id: string) => void; }
type BookingFilter = 'all' | 'active' | 'completed';
export const BookingStatusView: React.FC<BookingStatusViewProps> = ({ bookings, onBrowseServices, onCancelBooking, onAddReview, onConfirmCompletion, onOpenChat }) => {
  const [selectedBookingId, setSelectedBookingId] = useState<string | null>(null); const [filter, setFilter] = useState<BookingFilter>('all'); const [searchQuery, setSearchQuery] = useState(''); const [sortOrder, setSortOrder] = useState<'newest' | 'oldest' | 'price_high'>('newest'); const [copiedTxHash, setCopiedTxHash] = useState(false); const [copiedBookingId, setCopiedBookingId] = useState(false); const [reviewRating, setReviewRating] = useState(5); const [reviewComment, setReviewComment] = useState(''); const [reviewSubmitted, setReviewSubmitted] = useState(false);
  const filteredBookings = bookings.filter((b) => { if (filter === 'active' && !(b.status === 'Confirmed' || b.status === 'In Progress' || b.status === 'Pending')) return false; if (filter === 'completed' && !(b.status === 'Completed' || b.status === 'Cancelled')) return false; const query = searchQuery.trim().toLowerCase(); if (!query) return true; return [b.id, b.serviceName, b.status, b.providerName, b.clientName, b.piPaymentId].some((value) => String(value || '').toLowerCase().includes(query)); });
  const sortedBookings = [...filteredBookings].sort((a, b) => sortOrder === 'oldest' ? new Date(a.createdAt || 0).getTime() - new Date(b.createdAt || 0).getTime() : sortOrder === 'price_high' ? b.pricePi - a.pricePi : new Date(b.createdAt || 0).getTime() - new Date(a.createdAt || 0).getTime());
  const activeBooking = bookings.find((b) => b.id === selectedBookingId) || null;
  if (activeBooking) return <BookingDetailsView booking={activeBooking} onBack={() => setSelectedBookingId(null)} onCancelBooking={onCancelBooking} onAddReview={onAddReview} onConfirmCompletion={onConfirmCompletion} onOpenChat={onOpenChat} />;
  return <div className="max-w-2xl mx-auto space-y-5 pb-24 animate-in fade-in duration-200"><div className="space-y-1"><div className="flex items-center justify-between"><h1 className="text-xl font-black text-zinc-900 tracking-tight">My Bookings</h1><button type="button" onClick={onBrowseServices} id="btn-list-browse-more" className="text-xs font-bold text-amber-700 hover:text-amber-800 transition cursor-pointer">+ Book New Service</button></div><p className="text-xs text-zinc-500 font-medium">{bookings.length} service order{bookings.length > 1 ? 's' : ''} stored on Pi Network</p></div><div className="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3 pt-1"><div className="flex items-center gap-1.5 overflow-x-auto pb-1 no-scrollbar w-full sm:w-auto">{([{ id: 'all', label: 'All', count: bookings.length }, { id: 'active', label: 'Active', count: bookings.filter((b) => b.status === 'Confirmed' || b.status === 'In Progress' || b.status === 'Pending').length }, { id: 'completed', label: 'Completed', count: bookings.filter((b) => b.status === 'Completed' || b.status === 'Cancelled').length }] as const).map((tab) => <button key={tab.id} type="button" onClick={() => setFilter(tab.id)} className={`px-3 py-1.5 rounded-full text-xs font-extrabold transition shrink-0 flex items-center gap-1.5 cursor-pointer ${filter === tab.id ? 'bg-amber-600 text-white shadow-xs' : 'bg-zinc-100 text-zinc-600 hover:bg-zinc-200'}`}><span>{tab.label}</span><span className={`text-[10px] px-1.5 py-0.2 rounded-full font-black ${filter === tab.id ? 'bg-amber-700 text-amber-10' : 'bg-zinc-200 text-zinc-700'}`}>{tab.count}</span></button>)}</div><div className="flex items-stretch gap-0 rounded-2xl bg-white border border-zinc-200 overflow-hidden focus-within:border-orange-300 focus-within:ring-2 focus-within:ring-orange-500/10 transition w-full sm:w-auto"><div className="flex-1 min-w-0 flex items-center px-4 py-3 sm:w-44"><Search className="w-4 h-4 text-zinc-400 shrink-0 mr-2.5" /><input type="search" value={searchQuery} onChange={(e) => setSearchQuery(e.target.value)} placeholder="Search bookings" aria-label="Search bookings" className="w-full min-w-0 text-zinc-900 placeholder-zinc-400 text-xs sm:text-sm font-medium bg-transparent focus:outline-none" /></div><div className="flex items-center gap-2 px-3 sm:px-4 py-3 border-l border-zinc-200 text-zinc-700 text-xs font-bold shrink-0"><ArrowUpDown className="w-3.5 h-3.5 text-zinc-400" /><span className="hidden sm:inline">Sort:</span><select value={sortOrder} onChange={(e) => setSortOrder(e.target.value as any)} className="max-w-[96px] sm:max-w-none bg-transparent text-xs font-bold text-zinc-800 focus:outline-none cursor-pointer"><option value="newest">Newest First</option><option value="oldest">Oldest First</option><option value="price_high">Highest Price</option></select></div></div></div>{sortedBookings.length === 0 ? <div className="p-8 rounded-3xl bg-zinc-50 border border-zinc-200 text-center space-y-3"><AlertCircle className="w-8 h-8 text-zinc-400 mx-auto" /><div className="space-y-1"><h3 className="text-sm font-bold text-zinc-800">No Bookings Found</h3><p className="text-xs text-zinc-500">There are no booking records matching the current filters.</p></div><button type="button" onClick={() => { setFilter('all'); setSearchQuery(''); }} className="px-4 py-2 rounded-xl bg-zinc-200 hover:bg-zinc-300 text-zinc-800 text-xs font-bold transition cursor-pointer">Clear Filters</button></div> : <div className="divide-y divide-zinc-200 border-y border-zinc-200">{sortedBookings.map((b) => { const badge = getStatusBadge(b); return <div key={b.id} onClick={() => setSelectedBookingId(b.id)} id={`card-booking-item-${b.id}`} className="group py-4 first:pt-3 last:pb-3 cursor-pointer transition hover:bg-zinc-50/70"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-2 min-w-0"><span className={`inline-flex items-center gap-1.5 px-2.5 py-0.5 rounded-full text-[10px] font-extrabold border ${badge.classes}`}><span className={`w-1.5 h-1.5 rounded-full ${badge.dot}`} /><span>{badge.label}</span></span>{b.status === 'In Progress' && b.escrow_status === 'paid_escrowed' && <span className="hidden sm:inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-amber-50 text-amber-800 text-[10px] font-bold border border-amber-200/60"><span>Escrow Protected</span></span>}</div><div className="text-right shrink-0"><span className="text-base font-black text-amber-600 block">{b.pricePi.toFixed(2)} π</span><span className="text-[10px] text-zinc-400 block font-medium">Pi Escrow</span></div></div><div className="flex items-center justify-between gap-3 mt-2.5"><h3 className="text-sm font-black text-zinc-900 group-hover:text-amber-700 transition leading-snug truncate min-w-0">{b.serviceName}</h3><div className="flex items-center gap-1 text-[11px] font-bold text-amber-700 group-hover:translate-x-0.5 transition shrink-0"><span>View Details</span><ChevronRight className="w-4 h-4" /></div></div><div className="flex items-center gap-1.5 text-zinc-600 font-semibold text-xs mt-2 truncate"><Clock className="w-3.5 h-3.5 text-amber-600 shrink-0" /><span>Created: {formatBookingCreated(b.createdAt)}</span></div><ClientAcceptanceBanner booking={b} compact /></div>; })}</div>}</div>;
};
