import React, { useState } from 'react';
import { BusinessProfile, Provider, Service } from '../types';
import { providerMediaService } from '../services/providerMediaService';
import { BadgeCheck } from 'lucide-react';
export interface MerchantCardProps { merchant?: BusinessProfile | Provider | null; services?: Service[]; title?: string; badgeText?: string; onOpenAbout?: (merchant?: BusinessProfile | Provider) => void; actionLabel?: string; className?: string; showBadge?: boolean; }
export const MerchantCard: React.FC<MerchantCardProps> = ({ merchant, services = [], title, onOpenAbout, actionLabel = 'View Profile', className = '', showBadge = true }) => {
 const [imgError,setImgError]=useState(false); const isBusiness=!!merchant&&'avatarUrl' in merchant; const isProvider=!!merchant&&'fullName' in merchant; const rawName=title||(isBusiness?(merchant as BusinessProfile).name:undefined)||(isProvider?(merchant as Provider).fullName:undefined); const name=rawName?.trim()||'Service Merchant'; const rawPhoto=isBusiness?(merchant as BusinessProfile).avatarUrl:(isProvider?(merchant as Provider).photoUrl:undefined); const resolvedPhoto=providerMediaService.getMediaUrl(rawPhoto); const fallbackAvatar=`https://api.dicebear.com/7.x/initials/svg?seed=${encodeURIComponent(name)}&backgroundColor=ea580c,f97316&textColor=ffffff`; const avatarUrl=!imgError&&resolvedPhoto?resolvedPhoto.trim():fallbackAvatar; const headline=(isBusiness?((merchant as BusinessProfile).headline||(merchant as BusinessProfile).tagline):((merchant as Provider).headline||(merchant as Provider).roleTitle))||'Service Provider'; const rawBio=isBusiness?(merchant as BusinessProfile).bio:(merchant as Provider).bio; const bio=rawBio?.trim()||null; const specialties=(isBusiness?(merchant as BusinessProfile).specialties:(merchant as Provider).specialties)||[]; const skills=isProvider?(merchant as Provider).skills||[]:[]; const allTags=Array.from(new Set([...specialties,...skills])).slice(0,3); const rating=merchant?.rating; const reviewsCount=merchant?.reviewsCount; const hasRealRating=rating!=null&&Number(rating)>0&&reviewsCount!=null&&Number(reviewsCount)>0; const rawAvailability=isProvider?(merchant as Provider).availabilityStatus:isBusiness?(merchant as BusinessProfile).availabilityStatus:undefined; const availabilityStatus=rawAvailability==='available'||rawAvailability==='online'?'online':'offline'; const profileVerified=isProvider&&(merchant as Provider).profileVerified===true; const publishedServicesCount=services.filter(s=>s.status==='Published'&&(!isProvider||s.providerId===merchant?.id)).length; const serviceMode=isProvider?((merchant as Provider).serviceMode||'Remote'):'Global / Remote';
 return <div className={`relative rounded-2xl bg-zinc-50 hover:bg-orange-50/60 p-5 shadow-sm hover:shadow-md transition-all duration-200 flex flex-col justify-between text-zinc-900 space-y-4 ${className}`}><div className="space-y-3"><div className="grid grid-cols-[minmax(0,1fr)_auto] items-start gap-4"><div className="min-w-0 space-y-1 pt-0.5 text-left"><div className="flex items-center gap-1.5 min-w-0"><h3 className="text-base font-black tracking-tight text-zinc-900 truncate">{name}</h3>{showBadge&&profileVerified&&<BadgeCheck className="w-4 h-4 shrink-0 text-orange-600 fill-orange-600 stroke-white" strokeWidth={2.2} title="Verified by W3C Pi Bookings" aria-label="Verified by W3C Pi Bookings"/>}</div><p className="text-xs font-bold text-orange-600 line-clamp-1">{headline}</p><p className="text-[11px] text-zinc-500 font-medium truncate">{serviceMode}</p></div><div className="w-24 shrink-0 flex flex-col items-center gap-2"><div className="w-14 h-14 rounded-full bg-orange-100 p-0.5 overflow-hidden"><img src={avatarUrl} alt={name} onError={()=>setImgError(true)} className="w-full h-full object-cover rounded-full"/></div>{showBadge&&<span className={`inline-block max-w-full px-2.5 py-0.5 rounded-full text-[10px] font-extrabold uppercase tracking-wider text-center truncate ${availabilityStatus==='online'?'bg-emerald-50 text-emerald-800':'bg-zinc-100 text-zinc-600'}`}>{availabilityStatus}</span>}</div></div>{bio&&<p className="text-xs text-zinc-600 line-clamp-2 leading-relaxed font-normal text-left">{bio}</p>}{allTags.length>0&&<div className="flex items-center justify-start gap-1.5 flex-wrap pt-0.5">{allTags.map((tag,idx)=><span key={idx} className="inline-block text-[10px] font-bold text-zinc-700 bg-zinc-100 px-2.5 py-0.5 rounded-full">{tag}</span>)}</div>}</div><div className="pt-3 border-t border-zinc-100 flex items-center justify-between text-xs text-zinc-600"><div className="flex items-center gap-2">{hasRealRating?<div className="flex items-center gap-1 bg-amber-50 px-2 py-0.5 rounded-full"><span className="font-extrabold text-zinc-900">{Number(rating).toFixed(1)} ★</span><span className="text-zinc-500 font-medium text-[11px]">({reviewsCount})</span></div>:<span className="inline-block text-[10px] font-bold text-orange-950 bg-orange-100/70 px-2.5 py-0.5 rounded-full">{publishedServicesCount>0?`${publishedServicesCount} Services`:'Verified Provider'}</span>}</div>{onOpenAbout&&<button onClick={()=>onOpenAbout(merchant||undefined)} id={`btn-view-profile-${merchant?.id||'default'}`} className="px-4 py-1.5 rounded-full bg-orange-600 hover:bg-orange-500 text-white font-black text-xs transition cursor-pointer shrink-0">{actionLabel}</button>}</div></div>;
};